<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <groupId>org.openprovenance.prov</groupId>
        <artifactId>modules-tools</artifactId>
        <version>${provtoolboxVersion}</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>${rootArtifactId}-docker</artifactId>
    <name>|---- ${rootArtifactId}-docker</name>
    <packaging>jar</packaging>
    <description>A Docker image for the Templates service</description>
    <version>${version}</version>



    <properties>
        <service.context>ptl</service.context>
        <origin.project>${rootArtifactId}-service</origin.project>
        <docker.java.platform>temurin</docker.java.platform>
        <upload.directory>/home/pservice/files</upload.directory>  <!-- same folder as in docker profile in service-translator -->
        <cached.dictionary.folder>${basedir}/dictionary</cached.dictionary.folder>
        <deployed.dictionary.folder>${project.build.directory}/dictionary</deployed.dictionary.folder>
    </properties>

    <build>

        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>

        <plugins>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>${exec.plugin.version}</version>
                <executions>
                    <execution>
                        <id>exec1</id>
                        <phase>compile</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <workingDirectory>${project.build.directory}</workingDirectory>
                            <executable>make</executable>
                            <arguments>
                                <argument>-f</argument>
                                <argument>classes/Makefile</argument>
                                <argument>build.all</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <id>copy</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <artifactItems>
                        <artifactItem>
                            <groupId>${groupId}</groupId>
                            <artifactId>${rootArtifactId}-service</artifactId>
                            <version>${version}</version>
                            <classifier>secure</classifier>
                            <type>war</type>
                            <overWrite>true</overWrite>
                            <outputDirectory>${project.build.directory}</outputDirectory>
                        </artifactItem>
                    </artifactItems>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${failsafe.plugin.version}</version>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>${javadoc.plugin.version}</version>
                <configuration>
                    <source>1.9</source>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <copy file="${project.basedir}/docker.conf/gitignore"
                                      tofile="${project.basedir}/docker.conf/.gitignore"
                                      overwrite="false"/>
                                <copy file="${project.build.directory}/classes/env_file.env"
                                      tofile="${project.basedir}/docker.conf/env_file.env"
                                      overwrite="true"/>
                                <copy file="${project.build.directory}/classes/env_file.env"
                                      tofile="${project.basedir}/docker.conf/.env"
                                      overwrite="true"/>
                                <chmod file="${project.basedir}/docker.conf/docker-entrypoint-initdb16.d/01-init.sh" perm="755"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>


    </build>

</project>
